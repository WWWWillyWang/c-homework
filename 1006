#include "stdio.h"


int main()
{
	double input;
	while (scanf("%lf", &input) != EOF){
		input = input * 1000;
		input = (int)input;
		input = input / 1000;
		printf("%8.3lf\n", input);
	}

    return 0;
}